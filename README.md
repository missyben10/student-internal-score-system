# student-internal-score-system
A static web application that loads and displays student internal scores dynamically using JavaScript Fetch API with CSV dataset integration. Includes search, average calculation, and conditional formatting features.
